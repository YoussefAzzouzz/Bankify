{% extends 'backbase.html.twig' %}

{% block content %}
    <div class="container text-center">
        <div class="row">
            <div class="col">
                <img src="{{ qrCodeUri }}" />
            </div>
        </div>
    </div>
{% endblock %}
