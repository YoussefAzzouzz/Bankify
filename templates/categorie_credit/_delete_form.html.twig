<form method="post" action="{{ path('categorie_delete', {'id': categorie_credit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_credit.id) }}">
    <button class="btn">Supprimer</button>
</form>
