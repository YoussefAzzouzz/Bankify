<form method="post" action="{{ path('app_categorie_assurance_delete', {'id': categorie_assurance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_assurance.id) }}">
    <button class="btn">Delete</button>
</form>
