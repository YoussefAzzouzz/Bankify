{% extends 'base.html.twig' %}

{% block title %}Type
{% endblock %}

{% block body %}
	<h1>Type</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ type.id }}</td>
			</tr>
			<tr>
				<th>Type</th>
				<td>{{ type.type }}</td>
			</tr>
			<tr>
				<th>Montant_max</th>
				<td>{{ type.montantMax }}</td>
			</tr>
			<tr>
				<th>Montant_min</th>
				<td>{{ type.montantMin }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_typeC_index') }}">back to list</a>

	<a href="{{ path('app_typeC_edit', {'id': type.id}) }}">edit</a>

	{{ include('typeC/_delete_form.html.twig') }}
{% endblock %}
