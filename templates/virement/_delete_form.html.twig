<form method="post" action="{{ path('app_virement_delete', {'id': virement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" style="text-align: left;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ virement.id) }}">
    <button class="btn-delete" style="background-color: blue; color: white; padding: 8px 16px; border: none; border-radius: 4px;">Delete</button>
</form>
