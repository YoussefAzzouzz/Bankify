{% extends 'backbase.html.twig' %}

{% block title %}<title>New Carte</title>{% endblock %}

{% block content %}
    <div style="display: flex; justify-content: center;">
        <div>
            <h1>Ajouter une Carte</h1>

            <center>{{ include('carte/_form.html.twig') }}

            <a class="btn btn-primary" href="{{ path('app_carte_index') }}">back to list</a></center>
        </div>
    </div>
{% endblock %}
